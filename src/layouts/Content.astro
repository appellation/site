---
import Navbar from "../components/Navbar.astro";

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/poggers.webp" />
		<meta name="generator" content={Astro.generator} />
		<link rel="manifest" href="/manifest.json" />

		<meta name="twitter:card" content="summary" />
		<meta name="twitter:site" content="@appellation_" />

		<title>{title} | Will Nelson</title>
	</head>
	<body
		class="flex h-screen bg-stone-200 dark:bg-stone-800 dark:text-white font-serif"
	>
		<div class="container mx-auto w-full px-4">
			<Navbar />
			<article
				class="prose prose-stone dark:prose-invert max-w-prose mx-auto w-full"
			>
				<h1>{title}</h1>
				<slot />
			</article>
		</div>
	</body>
</html>
